# Disabled: LinenFlow is an Xcode project (LinenFlow.xcodeproj), not Swift Package Manager.
# Use .github/workflows/ios-ci.yml for iOS build and test on macos-15 runners.

name: Swift

on:
  workflow_dispatch:

jobs:
  build:
    if: false
    runs-on: macos-latest
    steps:
      - run: echo "This workflow is disabled. Use ios-ci.yml instead."
